<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FishMarketPlace" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/secondary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teel_200</item>
        <item name="colorSecondaryVariant">@color/teel_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="materialButtonStyle">@style/Button</item>
        <!-- Status bar color. -->
        <!--item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item-->
        <!-- Customize your theme here. -->
    </style>

    <style name="ToolbarStyle" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <item name="textAllCaps">true</item>
        <item name="titleTextColor">@color/species_background_green</item>
        <item name="backgroundColor">@color/species_background_green</item>
        <item name="background">@color/species_background_yellow</item>
    </style>

    <style name="Button" parent="Widget.AppCompat.Button.Colored">

        <item name="cardCornerRadius">100dp</item>

        <item name="android:paddingLeft">@dimen/padding_double</item>
        <item name="android:paddingRight">@dimen/padding_double</item>
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ButtonRed" parent="Button">

        <item name="backgroundTint">@color/species_background_red</item>
    </style>

    <style name="FullScreenFragment" parent="Theme.FishMarketPlace">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>